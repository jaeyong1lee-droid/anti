# KaTeX 미적용 및 빨간색 텍스트 표출 원인 분석 보고서

사용자님께서 첨부해주신 스크린샷 3장(`\dfrac` 수식이 빨간색으로 깨지는 현상, `S(\Delta t)` 및 `P_{cr}` 변수가 일반 텍스트로 노출되는 현상)을 정밀 분석한 결과, 각각 복합적인 원인이 얽혀 있음을 확인했습니다.

## 1. `\dfrac{...}` 수식이 빨간색(인라인 코드)으로 렌더링되는 원인
* **AI의 백틱(Backtick) 환각:** 프롬프트 지침(`마크다운 백틱 사용 엄격히 금지`)에도 불구하고, AI가 여전히 수식을 백틱(`` ` ``)으로 감싸서 출력하는 경우가 발생하고 있습니다. (예: `` `\dfrac{t}{S(\Delta t)}` ``)
* **정규식 치유와 파서의 충돌:** 서버의 `formulaRegex`가 이를 감지하여 백틱 안쪽에 `$ $`를 강제로 씌웁니다 (`` `$\dfrac{...}$` ``). 그러나 프론트엔드의 `react-markdown` 파서는 백틱을 **인라인 코드 블록(`<code>`)**으로 최우선 인식하기 때문에, 내부에 있는 `$`를 수식 기호로 취급하지 않고 무시해버립니다. 그 결과 KaTeX 렌더링이 작동하지 않고, CSS 스타일(빨간색 텍스트)만 적용되어 날것 그대로 표출됩니다.

## 2. `S(\Delta t)` 변수가 텍스트로 노출된 원인 (서버 정규식 누락)
* **정규식 범위 초과:** AI가 달러 기호 없이 `S(\Delta t)`를 출력했을 때, 서버의 `simpleVariableRegex`가 이를 감지해 `$ $`를 씌워주어야 합니다. 
* 그러나 현재 함수 기호를 감지하는 정규식 패턴(`\b[a-zA-Z]\([a-zA-Z0-9_']+\)`)은 괄호 안에 **영문자, 숫자, 언더바**만 허용하도록 매우 엄격하게 짜여 있습니다. `\Delta t`에는 역슬래시(`\`)와 공백( )이 포함되어 있어 정규식 매칭에 실패했고, 치유되지 않은 채 텍스트로 전달되었습니다.

## 3. `(P_{cr})` 변수가 텍스트로 노출된 원인 (HTML 렌더링 로직 충돌)
* **글머리 기호(List) 강제 HTML 변환 충돌:** 서버에서는 `P_{cr}`을 감지해 `$P_{cr}$`로 정상 복구하여 넘겼지만, 프론트엔드 파이프라인에서 문제가 발생했습니다.
* 프론트엔드의 `renderingHelpers.js` (`preprocessLatex` 함수)는 마크다운의 글머리 기호(`-`나 `1.`)를 감지하면, 이를 직접 `<ul><li>...</li></ul>` 형태의 날것 HTML 태그로 변환해버립니다.
* **Markdown 파서의 한계:** 변환된 `<li>` 태그 뭉치를 `react-markdown`에 넘기는데, 마크다운 표준 규칙상 **블록 레벨 HTML 태그(예: `<li>`, `<div>`) 내부에 있는 마크다운 문법이나 수식(`$ $`)은 파싱이 무시**됩니다. 따라서 KaTeX가 `$P_{cr}$`을 수식으로 인식하지 못하고 그냥 텍스트로 넘겨버린 것입니다.

---
**요약하자면:**
1. AI의 백틱(`` ` ``) 환각을 서버에서 강제로 벗겨내는 로직이 누락됨.
2. `S(\Delta t)` 같은 복잡한 기호를 서버가 자동 치유(Auto-healing) 정규식에서 놓침.
3. 프론트엔드에서 리스트(`-`)를 강제로 `<li>` 태그로 변환하는 바람에, 그 안에 포함된 수식들의 KaTeX 파싱이 셧다운됨.

사용자님의 지시("원인분석 코딩은 대기해")에 따라 실제 코드 수정은 홀드(대기)하고 있습니다. 분석 내용 확인 후, 코딩 및 수정 방향에 대한 지시를 내려주시면 즉각 조치하겠습니다!
