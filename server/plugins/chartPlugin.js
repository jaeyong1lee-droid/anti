export const CHART_DIAGRAM_PROMPT = `
[📈 인터랙티브 차트그래프 작도 지침 (ASCII 아트 및 직접 SVG 작도 절대 금지)]:
1. ⚠️ [절대 철칙]: 사용자가 "그래프를 그려줘", "차트그래프를 그려줘"라고 요청한 경우, **절대로 별표(*), 하이픈(-), 슬래시(/) 등을 이용한 아스키 아트(ASCII Art)를 그리거나 \`\`\`svg\`\`\` 태그를 직접 작성하지 마십시오.**
2. 시각적인 2D 그래프(응력-변형률 곡선, 수명-시간 곡선 등)가 필요한 경우, 프론트엔드의 Recharts 엔진이 자동으로 인터랙티브 툴팁과 범례를 렌더링할 수 있도록 **반드시 마크다운 코드 블록 식별자인 \`\`\`chart\`\`\` 를 사용하여 완전한 JSON 데이터만 출력**하십시오.
3. [✅ 데이터 구조 필수 규격]: JSON 데이터는 다음 형식을 완벽하게 준수해야 합니다:
   \`\`\`chart
   {
     "title": "그래프 한글 제목",
     "xAxisLabel": "X축 레이블 (예: 시간 $t$)",
     "yAxisLabel": "Y축 레이블 (예: 응력 $\\\\sigma$)",
     "description": "그래프가 의미하는 주요 공학적 원리, 변곡점, 기울기, 또는 y=ax+b와 같은 수식 및 절편의 의미를 설명하는 해설 텍스트 (반드시 한 줄로 작성할 것)",
     "lines": [
       { "name": "실측 데이터", "dataKey": "y1", "stroke": "#38bdf8" },
       { "name": "추세선", "dataKey": "y2", "stroke": "#f43f5e" }
     ],
     "data": [
       { "x": "0", "y1": 10, "y2": 10 },
       { "x": "5", "y1": 25, "y2": 22 },
       { "x": "10", "y1": 45, "y2": 40 }
       // ... 부드러운 곡선 표현을 위해 반드시 x축 데이터를 최소 15개 이상으로 촘촘하게 생성할 것
     ]
   }
   \`\`\`
   - 🚨 **[JSON 배열 문법 절대 철칙]**: "lines" 및 "data" 배열 내부에는 오직 순수한 객체({})들만 나열해야 합니다! 배열 내부에 키(Key) 값(예: "추세선" : { ... })을 선언하는 치명적인 JSON 문법 오류를 절대 범하지 마십시오.
4. [수식(KaTeX) 적용 및 JSON 이스케이프 철칙]:
   - \`xAxisLabel\`, \`yAxisLabel\`, \`name\`(범례 항목), \`x\`(X축 눈금) 값 내부에 수학 기호나 그리스 문자(예: $\\\\sigma$, $\\\\epsilon$, $t_0$ 등)가 들어갈 경우 인라인 수식 \`$ ... $\`을 자유롭게 사용하십시오.
   - ⚠️ [치명적 오류 주의]: JSON 문자열 내부에서 백슬래시(\\)를 사용할 때는 **반드시 이중 백슬래시(\\\\\\\\)로 이스케이프**해야 합니다. (잘못된 예: "$\sigma$", 올바른 예: "$\\\\sigma$")
   - 🚨 [절대 금지 - 환각/오타 방지]: 분수나 나눗셈을 표현할 때 슬래시에 아래첨자를 붙이는 기괴한 방식(예: $t/_{S_t}$, $t/_S$)이나 유니코드 특수 첨자(예: ᵗ, ₛ)를 절대 사용하지 마십시오. 분수는 반드시 표준 LaTeX 문법인 \`\\\\frac{분자}{분모}\`를 사용하고, 첨자는 \`_\` 와 \`^\` 만을 사용하십시오.
5. [데이터 스케일링 규칙 및 그래프 해상도 철칙 (매우 중요)]:
   - \`data\` 배열의 \`x\` 값은 문자열 또는 숫자로 자유롭게 입력하되 순서대로 입력해야 합니다. \`y1\`, \`y2\` 등 \`dataKey\`에 매핑되는 값은 **반드시 쌍따옴표 없는 실수 또는 정수(Number) 형태의 순수 숫자만 입력**하십시오. 단위(예: %, kN)나 텍스트를 숫자에 직접 붙여 넣으면 절대 안 됩니다! (잘못된 예: \`"y1": 직경의1%\` 🚨 문법 에러 유발). 단위를 표시하고 싶다면 축 레이블(Label) 필드에만 기재하십시오.
   - 🚨 **그래프의 곡선이 부드러운 일정한 선으로 표현되도록 데이터 간격을 촘촘하게 설정하여 x축 데이터(data 배열의 항목 수)를 반드시 15개 이상으로 생성하십시오.** (기존 7개 수준의 듬성듬성한 데이터 산출 금지)
`;
